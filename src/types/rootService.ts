export type originSongInfo = {
  index: number;
  id: string;
};

export type finalSongInfo = {
  albumPost: string;
  albumName: string;
  created: string;
  type: string;
  artist: string;
  title: string;
};
